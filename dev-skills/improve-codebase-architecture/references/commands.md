# Comandos (reference) — Improve Codebase Architecture

Detalle local de `SKILL.md` (## References).

```bash
# Quick structure overview
find lib -type f -name "*.dart" | head -20
tree -L 3 src/ 2>/dev/null || find src -type d | head -20
