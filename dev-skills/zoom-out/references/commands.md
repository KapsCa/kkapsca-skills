# Comandos (reference) — Zoom Out

Detalle local de `SKILL.md` (## References).

```bash
# Ver imports de un archivo (Dart)
grep -R "^import" lib/features/my_feature/

# Ver dependencias de un paquete (Node)
npm list --depth=1

# Ver módulos que usan un paquete (Go)
grep -r "mi_paquete" .
